# Spark of Hope

Standalone Vercel root for the Spark of Hope MVP.

The app uses the shared Life Produces Life Supabase spine through public browser
environment variables only. Do not add a Supabase service role key to this
browser app unless a server-only route explicitly needs it.
